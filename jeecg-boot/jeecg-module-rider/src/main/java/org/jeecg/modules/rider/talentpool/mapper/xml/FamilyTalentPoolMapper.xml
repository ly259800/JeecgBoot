<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.rider.talentpool.mapper.FamilyTalentPoolMapper">

    <select id="pageList" resultType="org.jeecg.modules.rider.talentpool.dto.FamilyTalentPoolDTO">
        select ifnull(rc.name,tp.name) name,tp.*,(select name from rider_customer where id = rc.reference) promoter_name
        from family_talent_pool tp
        left join rider_customer rc on tp.customer_id = rc.id
        ${ew.customSqlSegment}
    </select>

    <select id="queryList" resultType="org.jeecg.modules.rider.talentpool.dto.FamilyTalentPoolDTO">
        select ifnull(rc.name,tp.name) name,tp.*,(select name from rider_customer where id = rc.reference) promoter_name
        from family_talent_pool tp
         left join rider_customer rc on tp.customer_id = rc.id
        ${ew.customSqlSegment}
    </select>


</mapper>